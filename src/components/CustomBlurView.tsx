import React from 'react';
import {CustomBlurViewTypes} from '../types/CustomBlurViewTypes';
import {Text, View} from 'react-native';

function CustomBlurView({style}: CustomBlurViewTypes) {
  return (
    <View style={style}>
      <Text style={{color: 'white'}}>Not implemented</Text>
    </View>
  );
}

export default CustomBlurView;
