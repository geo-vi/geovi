import styled from 'styled-components';
import {View} from 'react-native';

const SidebarSectionLeed = styled(View)`
  background-color: #2a2a2a;
  height: 2px;
  flex: 1;
`;

export default SidebarSectionLeed;
